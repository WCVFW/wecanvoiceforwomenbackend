name: 🚀 Deploy Spring Boot Backend to EC2

on:
  push:
    branches: [main]  # Deploys on every push to main branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: ✅ Checkout Code
        uses: actions/checkout@v3

      - name: ☕ Set up Java
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: 🏗️ Build Spring Boot App
        run: mvn clean package -DskipTests

      - name: 📁 List built JAR
        run: ls -la target

      - name: 🚀 Upload JAR to EC2
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_KEY }}
          port: 22
          source: "target/*.jar"
          target: "/home/ubuntu/"

      - name: 🧠 Rename & Restart Backend on EC2
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_KEY }}
          port: 22
          script: |
            echo "🔁 Renaming latest JAR to myapp.jar"
            LATEST_JAR=$(ls -t /home/ubuntu/*.jar | head -n 1)
            mv "$LATEST_JAR" /home/ubuntu/myapp.jar

            echo "🧹 Cleaning old logs"
            rm -f /home/ubuntu/app.log

            echo "🚀 Restarting backend service"
            sudo systemctl daemon-reexec
            sudo systemctl restart myapp

            echo "✅ Spring Boot backend deployed!"
